if hash docker-machine 2>/dev/null; then
  export DOCKER_MACHINE=$(docker-machine ls -q)

  if [ ! -z $DOCKER_MACHINE ]; then
    eval $(docker-machine env $DOCKER_MACHINE)
    echo "Docker Host: $DOCKER_HOST"
  fi

fi
